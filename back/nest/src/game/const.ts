const BallSpeed = 150;
const ballMomentumStart = 1.15;
const MaxBallXVelocity = 0.00009;
const ballAlpha = Math.PI / 6;
const EndPoint = 3;

export { BallSpeed, ballMomentumStart, MaxBallXVelocity, ballAlpha, EndPoint };
